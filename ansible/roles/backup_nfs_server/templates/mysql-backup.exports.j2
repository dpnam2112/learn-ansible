# Generated by Ansible
{% for h in backup_exports %}
{{ backup_nfs_root }}/{{ h.name }} \
  {% for c in h.clients -%}
  {{ c }}(rw,sync,no_subtree_check,root_squash,secure,anonuid={{ backup_service_uid }},anongid={{ backup_service_gid }}){% if not loop.last %} {% endif %}
  {%- endfor %}
{% endfor %}

