# Minimal GitLab Runner config — intentionally empty of [[runners]]
# You can safely replace this later with your full config.toml.
# Runner will stay idle until runners are registered/defined.

concurrent = 1
check_interval = 0

[session_server]
  session_timeout = 1800
